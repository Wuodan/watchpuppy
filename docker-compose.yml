services:
  watchpuppy:
    build:
      context: .
    image: wuodan/watchpuppy:latest
    container_name: watchpuppy
    environment:
      LOG_LEVEL: INFO
    volumes:
      - ./input/:/data/input:ro